<div class="container">
    <div class="container-child col-md-2 col-sm-4 col-xs-6">
      <h4><%= @user.name %><br>フォロー一覧</h4>
      <%= render 'followed_form', user: @user  %>
      <h4><%= @user.name %><br>フォロワー一覧</h4>
      <%= render 'follower_form', user: @user  %>
    </div>
</div>
